import React from "react";
import PetCard from "../components/PetCard/PetCard";

const Match = () => {
  return (
    <div className="match">
      <PetCard />
    </div>
  );
};

export default Match;
